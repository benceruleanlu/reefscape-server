{
  "name": "reefscape-server",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "tsc && node build/app.js",
    "watch": "tsc --watch",
    "newkeys": "openssl req -newkey rsa:2048 -nodes -keyout server-key.pem -x509 -out server-cert.pem",
    "prepare": "husky"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@types/node": "^22.9.0",
    "husky": "^9.1.7",
    "typescript": "^5.6.3"
  }
}
